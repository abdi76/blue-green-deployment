apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: blue-green-demo
data:
  database.host: "db.example.com"
  database.port: "5432"
  cache.enabled: "true"
  log.level: "info"
  app.environment: "production"
